# Mission 15: Gobuster - Tìm ẩn số (Directory Brute-force)

Hướng dẫn sử dụng `gobuster` để dò đường dẫn ẩn trên web server và ví dụ cơ bản.

```bash
gobuster dir -u https://example.com -w /path/to/wordlist.txt
```
