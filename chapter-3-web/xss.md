# Mission 17: XSS (Cross-Site Scripting) - Ảo thuật trên trình duyệt

Giới thiệu XSS (Reflected, Stored, DOM-based), ví dụ payload và cách test an toàn trong lab.

(Chú ý: XSS có thể gây rò rỉ session — chỉ thử nghiệm trong môi trường kiểm soát.)
