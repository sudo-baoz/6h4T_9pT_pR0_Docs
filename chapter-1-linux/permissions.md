# Mission 5: Quyền lực tối thượng (Sudo & Permissions)

Giải thích `chmod`, `chown`, quyền user/group, và cách sử dụng `sudo` an toàn.

- Ví dụ: `chmod 750 script.sh`
- Thực hành: tạo user test và thử thay đổi quyền.
