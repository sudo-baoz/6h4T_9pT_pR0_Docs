# Mission 8: Cài đặt đồ chơi (Apt & Git)

Hướng dẫn sử dụng `apt` để cài gói, cập nhật hệ thống và cơ bản về `git` cho quản lý mã nguồn.

- Cập nhật: `sudo apt update && sudo apt upgrade`
- Cài package: `sudo apt install <package>`
- Git: `git clone`, `git commit`, `git push`
