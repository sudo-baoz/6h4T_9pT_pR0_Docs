# Quét mạng bằng nmap

Giới thiệu cơ bản về `nmap`, các kiểu quét phổ biến và ví dụ lệnh:

- Quét cổng TCP nhanh: `nmap -sS <target>`
- Quét port và phiên bản: `nmap -sV <target>`

(Thêm ví dụ thực hành và cảnh báo pháp lý.)
